{
  "name": "api-scanner-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend API for API-Scanner job orchestration and server-side scanning",
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
