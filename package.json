{
  "name": "api-scanner",
  "private": true,
  "version": "1.0.0",
  "description": "Client-side web security scanner — secrets detection, header auditing, SQLi, NoSQL, and XSS testing. No backend, no tracking.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
